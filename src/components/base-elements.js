import styled from "@emotion/styled";

const Container = styled.div`
  text-align: center;
`;
const Content = styled.div`
  width: 100%;
  max-width: 1024px;
  text-align: left;
  display: inline-block;
`;
const Header = styled.header`
  margin: 0;
  padding: 1.5rem;
  text-align: left;
`;

const Hero = styled.div`
  margin: 0.5rem 0;
  padding: 0 2rem;
`;

const Section = styled.div`
  padding: 0 2rem;
`;

export { Container, Content, Header, Hero, Section };
